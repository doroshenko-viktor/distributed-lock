// use super::{request::Request, response::Response, SocketData, StreamHandlingError};
// use std::convert::TryFrom;

// pub fn handle(socket: &mut SocketData) -> Result<Response, StreamHandlingError> {
//     let request = Request::try_from(socket)?;
//     println!("Handling request: {}", &request);
//     Ok()
// }
